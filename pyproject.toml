[project]
name = "ghostwatch"
version = "0.1.0"
description = "AI Security Probe Engine"
requires-python = ">=3.10"

dependencies = [
    "typer",
    "rich"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
ghostwatch = "src.core.cli:app"
